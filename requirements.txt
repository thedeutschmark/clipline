flask>=2.0.0
yt-dlp>=2023.0.0
Pillow>=12.0
typing_extensions
waitress
PySide6>=6.7

# Optional: captioning pipeline (installed lazily into a managed venv
# at first run via the in-app Dependency Setup panel — not bundled
# into the EXE).
# faster-whisper>=1.0.0
# pyannote.audio>=3.1
# torch>=2.0
